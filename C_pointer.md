# C 포인터 소소한 정리

## 1. 포인터 기초

### 포인터 선언과 역참조

```
int x = 10;
int *p = &x; // 포인터 변수 p에 변수 x의 메모리 주소를 저장

```

* `p`: 변수 `x`의 **메모리 주소**가 저장됩니다. (`p == &x`)

* `*p`: `p`가 가리키는 주소에 저장된 **값**을 의미합니다. (`*p == x`)

* `*p = 20;`과 같이 값을 변경하면 `p`가 가리키고 있는 `x`의 실제 값도 `20`으로 바뀝니다.

### 초기화 및 NULL 검사

* 포인터는 선언 시 반드시 초기화해 주는 것이 안전합니다.

* NULL로 초기화 했을 경우

```
int *p = NULL; 
```

* 포인터가 아무것도 가리키지 않는지 확인할 때는 비교 연산자(`==`)를 사용합니다.

```
if (p == NULL) {
    // p가 가리키는 대상이 없을 때 실행
}
```

## 2. 연산자 우선순위 (`*p++` vs `(*p)++`)

* 후위 증가 연산자(`++`)가 역참조 연산자(`*`)보다 **우선순위가 높습니다**.

```
*p++   // *(p++) 로 해석됨
(*p)++ // *p에 할당된 값을 1 증가시킴
```

* **`*p++`**: 현재 `p`가 가리키는 위치의 **값을 읽은 뒤**, 포인터 `p`의 주소 자체를 다음 위치로 이동시킵니다.

* **`(*p)++`**: 포인터가 가리키는 **대상 변수의 값 자체를 1 증가**시킵니다.

* 의도한 동작에 맞게 괄호 `()`를 정확하게 사용해야 합니다.

## 3. 포인터 산술과 형 변환

### 포인터 가산

포인터에 $n$을 더하는 `p + n`은 단순히 메모리 주소 숫자에 $n$을 더하는 것이 아니라, 포인터가 가리키는 타입의 크기만큼 이동하는 연산입니다.

$$
\text{이동 바이트 수} = n \times \text{sizeof}(*p)
$$

### 형 변환(Casting)에 따른 바이트 차이

`int` 배열을 가리키는 포인터 `p`가 있을 때 (`sizeof(int) == 4` 기준):

1. **`(char *)(p + 1) - (char *)p`**

   * `char*` 단위($1\text{ 바이트}$)로 주소 차이를 계산하므로 결과는 $4$가 됩니다.

2. **`(int *)(p + 1) - (int *)p`**

   * `int*` 단위(원소 $1\text{개}$)로 차이를 계산하므로 결과는 $1$이 됩니다.

## 4. `const`와 포인터

`const`는 constant(상수)의 약자로, 해당 대상을 읽기 전용(Read-Only)으로 지정합니다. `const`가 위치하는 자리에 따라 제한하는 대상이 달라집니다.

### 1) 가리키는 값이 고정되는 경우 (상수 가리키기)

```
const int *p1;
// 또는
int const *p1;

```

* **값 변경 불가:** `*p1 = 30;` (불가)

* **주소 변경 가능:** `p1 = &b;` (가능)

### 2) 포인터 주소 자체가 고정되는 경우 (상수 포인터)

```
int * const p2 = &a;

```

* **값 변경 가능:** `*p2 = 30;` (가능)

* **주소 변경 불가:** `p2 = &b;` (불가)

### 3) 주소와 값 모두 고정되는 경우

```
const int * const p3 = &a;

```

* 주소 변경과 포인터를 통한 값 변경 **모두 불가능**합니다.

### 요약표

| 구문 | 주소 변경 (`p`) | 가리키는 값 변경 (`*p`) | 
 | ----- | ----- | ----- | 
| `const int *p` | **가능** | **불가능** | 
| `int *const p` | **불가능** | **가능** | 
| `const int *const p` | **불가능** | **불가능** | 
